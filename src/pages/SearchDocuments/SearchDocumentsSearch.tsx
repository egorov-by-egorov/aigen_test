import React, { FunctionComponent } from 'react';

interface OwnProps {
}

type Props = OwnProps;

const SearchDocumentsSearch: FunctionComponent<Props> = ( props ) => {

    return (
        <h1>search</h1>
    );
};

export default SearchDocumentsSearch;
